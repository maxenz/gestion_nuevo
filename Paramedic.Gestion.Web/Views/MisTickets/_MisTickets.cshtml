@using System.Web.Mvc.Html;
@using Paramedic.Gestion.Web.HtmlHelpers;
@model PagedList.IPagedList<Paramedic.Gestion.Model.Ticket>
@using PagedList.Mvc;

<div id="misTicketsList">
    <div class="row">
        <div class="span10 offset1" style="margin-bottom: -30px">
            <div class="widget widget-table action-table">
                <div class="widget-header">
                    <i class="icon-th-list"></i>
                    <h3>Tickets</h3>
                </div>
                <!-- /widget-header -->
                <div class="widget-content">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th class="span3">@Html.DisplayNameFor(model => model.FirstOrDefault().UpdatedDate) </th>
                                <th class="span2">@Html.DisplayNameFor(model => model.FirstOrDefault().Usuario)</th>
                                @*<th class="span2">@Html.DisplayNameFor(model => model.FirstOrDefault().Cliente) </th>*@
                                <th class="span2">@Html.DisplayNameFor(model => model.FirstOrDefault().Asunto) </th>
                                <th class="span2">@Html.DisplayNameFor(model => model.FirstOrDefault().Asunto) </th>
                                <th class="span1"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.UpdatedDate)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Usuario.UserName)
                                    </td>
                                    @*<td>
                                            @Html.ActionLink(item.Cliente, "Edit", "Clientes", new { id = item.ClienteID }, null)
                                        </td>*@
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Asunto)
                                    </td>

                                    @TicketHtmlHelper.getTicketState(item.TicketEstadoType)

                                    <td class="centrado">
                                        <a href="MisTickets/Edit/@item.Id" class="btn btn-small btn-success btn-actions"><i class="btn btn-icon-only fa fa-edit"></i></a>
                                    </td>
                                </tr>

                            }

                        </tbody>
                    </table>
                </div>
                <!-- /widget-content -->
            </div>

        </div>
    </div>
    <div class="row">
        <div class="pagedList span7 offset5" data-gestion-target="#misTicketsList">

            @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))

        </div>

        <!-- /row -->
    </div>
</div>
<!-- /container -->
