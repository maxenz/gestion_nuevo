@model PagedList.IPagedList<Paramedic.Gestion.Web.ViewModels.ClienteViewModel>
@using PagedList.Mvc;

<div class="main">
    <div class="main-inner">
        <div class="container">
            <div class="row" >
                <div class="col-xs-12">
                    <div class="widget">
                        <div class="widget-header">
                            
                            <h3>Filtros</h3>
                        </div>
                        @*<div class="widget-content" style="margin-top: -10px; padding-bottom: 10px">
                            <form action="@Url.Action("Index")" method="get" id="frmSearchClientes" class="searchIndex" data-gestion-ajax="true" data-gestion-target="#clientesList" style="margin-bottom:-10px">
                                <div class="row">
                                    <div class="col-xs-2">
                                        <select id="selTipoClientes" name="selTipoClientes" style="width:100%">
                                            <option value="1">Todos</option>
                                            <option value="2">Vendidos</option>
                                            <option value="3">En Gesti&oacute;n</option>
                                        </select>
                                    </div>
                                    <div class="col-xs-2">
                                        <select id="selDatosSegunVista" name="selDatosSegunVista" style="width:100%">
                                            <option value="1">Datos Principales</option>
                                            <option value="2">&Uacute;ltima Gesti&oacute;n</option>
                                        </select>
                                    </div>
                                    <div class="col-xs-3">
                                        <input type="text" class=" col-xs-3" id="searchName" name="searchName" placeholder="Buscar..." style="width: 90%">
                                    </div>
                                    <div class="col-xs-1">
                                        <button class="btn btn-primary btn-small btn-actions" style="width: 100%" type="submit" id="search_button" name="search_button"><i class="btn-icon-only fa fa-search"></i></button>
                                    </div>
                                    <div class="col-xs-1">
                                        <a href="@Url.Action("Create", "Clientes")" class="btn btn-primary btn-small btn-actions" style="width:80%">
                                            <i class="btn-icon-only fa fa-plus"></i>
                                        </a>
                                    </div>
                                </div>
                            </form>
                        </div>*@

                        <div class="widget-content" style="margin-top: -10px; padding-bottom: 10px">
                            <form action="@Url.Action("Index")" method="get" class="searchIndex" data-gestion-ajax="true" data-gestion-target="#clientesList" style="margin-bottom:0px">
                                <div class="row">
                                    <div class="col-xs-6">
                                        <div class="input-group">
                                            <input class="form-control" placeholder="Buscar..." name="searchName">
                                            <span class="input-group-btn">
                                                <button class="btn btn-success btn-outline" type="submit">
                                                    <i class="fa fa-search"></i>
                                                </button>
                                            </span>
                                        </div>
                                    </div>

                                    <a href="@Url.Action("Create", "Clientes")" class="btn btn-success btn-outline">
                                        Agregar cliente
                                    </a>

                                    <select id="selTipoClientes" name="selTipoClientes" class="btn btn-success btn-outline">
                                        <option value="1">Todos</option>
                                        <option value="2">Vendidos</option>
                                        <option value="3">En Gesti&oacute;n</option>
                                    </select>

                                    <select id="selDatosSegunVista" name="selDatosSegunVista" class="btn btn-success btn-outline">
                                        <option value="1">Datos Principales</option>
                                        <option value="2">&Uacute;ltima Gesti&oacute;n</option>
                                    </select>
                                </div>
                            </form>
                        </div>


                    </div>
                </div>
            </div>
            @Html.Partial("_Clientes", Model)
        </div>
    </div>
</div>
