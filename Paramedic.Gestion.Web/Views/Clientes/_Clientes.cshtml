@model PagedList.IPagedList<Paramedic.Gestion.Web.ViewModels.ClienteViewModel>
@using PagedList.Mvc;

<div id="clientesList">
        <div class="col-xs-12">
            <div class="widget widget-table action-table" style="margin-bottom:5px">
                <div class="widget-header">
                    <h3>Clientes</h3>
                </div>

                <div class="widget-content">
                    <table class="table table-striped table-bordered fixed">
                        <thead>
                            <tr>
                                <th class="col-xs-2">@Html.DisplayNameFor(model => model.First().RazonSocial) </th>
                                <th class="col-xs-2">@Html.DisplayNameFor(model => model.First().Email)</th>
                                <th class="col-xs-2">@Html.DisplayNameFor(model => model.First().Telefono) </th>
                                <th class="col-xs-2">@Html.DisplayNameFor(model => model.First().Pais) </th>
                                <th class="col-xs-2">@Html.DisplayNameFor(model => model.First().Provincia) </th>
                                <th class="col-xs-2">@Html.DisplayNameFor(model => model.First().Localidad) </th>
                                <th class="col-xs-1"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="cliente">
                                        @Html.DisplayFor(modelItem => item.RazonSocial)
                                    </td>
                                    <td class="cliente">
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    <td class="cliente">
                                        @Html.DisplayFor(modelItem => item.Telefono)
                                    </td>
                                    <td class="cliente">
                                        @Html.DisplayFor(modelItem => item.Pais)
                                    </td>
                                    <td class="cliente">
                                        @Html.DisplayFor(modelItem => item.Provincia)
                                    </td>
                                    <td class="cliente">
                                        @Html.DisplayFor(modelItem => item.Localidad)
                                    </td>
                                    <td class="centrado cliente">
                                        <a href="@Url.Action("Edit", "Clientes", new { id = item.Id })" class="btn btn-small btn-success btn-actions">
                                            <i class="btn-icon-only fa fa-edit"></i>
                                        </a>
                                        <a href="@Url.Action("Delete","Clientes", new { id = item.Id })" class="btn btn-small btn-success btn-actions delete" data-gestion-target="#clientesList">
                                            <i class="btn-icon-only fa fa-times"></i>
                                        </a>
                                    </td>
                                </tr>

                            }

                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    <div class="pagedList centered clientes" data-gestion-target="#clientesList">
        @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
    </div>
</div>
