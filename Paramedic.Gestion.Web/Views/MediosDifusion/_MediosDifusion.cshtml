@model PagedList.IPagedList<Paramedic.Gestion.Model.MedioDifusion>
@using PagedList.Mvc;

<div id="mediosDifusionList">
    <div class="row">
        <div class="col-xs-10 col-xs-offset-1">
            <div class="widget widget-table action-table" style="margin-bottom:0">
                <div class="widget-header">

                    <h3>Medios de Difusi&oacute;n</h3>
                </div>

                <div class="widget-content">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th class="col-xs-5">@Html.DisplayNameFor(model => model.First().Descripcion) </th>
                                <th class="col-xs-2">@Html.DisplayNameFor(model => model.First().MapaVisible)</th>
                                <th class="col-xs-1"></th>
                                <th class="col-xs-1"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Descripcion)
                                    </td>
                                    <td>
                                        @if (item.MapaVisible)
                                        {
                                            <span style="color: green; font-weight: bold">
                                                SI
                                            </span>
                                        }
                                        else
                                        {
                                            <span style="color: red; font-weight: bold">
                                                NO
                                            </span>
                                        }
                                    </td>
                                    <td class="centrado">
                                        <a href="@Url.Action("Edit", "MediosDifusion", new { id = item.Id })" class="btn btn-small btn-success btn-actions">
                                            <i class="btn-icon-only fa fa-edit"></i>
                                        </a>
                                    </td>
                                    <td class="centrado">
                                        <a href="@Url.Action("Delete","MediosDifusion", new { id = item.Id })" class="btn btn-small btn-success btn-actions delete" data-gestion-target="#mediosDifusionList">
                                            <i class="btn-icon-only fa fa-times"></i>
                                        </a>
                                    </td>
                                </tr>

                            }

                        </tbody>
                    </table>
                </div>

            </div>

        </div>
    </div>
    <div class="pagedList centered" data-gestion-target="#mediosDifusionList">
        @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
    </div>
</div>

